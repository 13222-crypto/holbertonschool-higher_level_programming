# Python - Test-driven development

This project focuses on Test-Driven Development (TDD) using Python's `doctest` module. It emphasizes writing documentation and interactive tests before implementation to catch edge cases early.

## Learning Objectives

By the end of this project, you should be able to explain:
* What's an interactive test.
* Why tests are important.
* How to write Docstrings to create tests.
* How to write documentation for each module and function.
* What are the basic option flags to create tests.
* How to find edge cases.

## Requirements

* **OS/Environment:** Ubuntu 20.04 LTS interpreted with `python3` (version 3.8.5).
* **Code Style:** Must strictly follow `pycodestyle` (version 2.7.*).
* **Files Ending:** All files must end with a new line.
* **Executables:** All Python scripts must be executable (`chmod +x`).
* **Tests Folder:** All test cases must be `.txt` files located inside a folder named `tests/`.

## Tasks Summary

| Task | Files | Description |
| --- | --- | --- |
| **0. Integers addition** | `0-add_integer.py`, `tests/0-add_integer.txt` | A function that validates, casts, and adds two integers/floats. |

